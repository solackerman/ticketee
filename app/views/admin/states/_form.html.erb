<%= form_for [:admin, @state] do |f| %>
	<p>
		<%= f.label :name %>
		<%= f.text_field :name %>
	</p>
	<p>
		<%= f.label :color %>
		<%= f.text_field :color %>
	</p>
	<p>
		<%= f.label :background%>
		<%= f.text_field :background %>
	</p>
	<%= f.submit %>
<% end %>